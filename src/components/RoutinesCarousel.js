import React from 'react'
import styled from 'styled-components'

const RoutinesCarousel = styled.section`
`

export default () => {
    return (
        <RoutinesCarousel>
            
        </RoutinesCarousel>
    )
}
